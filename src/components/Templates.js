class Templates extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        return(
            <div id="templatePopup">
                {/* Ask which template you want to choose */}
                {/* You can configure which "widgets" you want yourself anyways and change the pane expirence */}
                {/* Classic pane (normal expirence) */}
                {/* Workout template, includes calender widget where you can put when you did your workouts and
                includes recommended workouts and more */}
                {/* Homework template/study, includes lofi music, a different pane expirence where you can make lists and check them off, it's slightly different */}
            </div>
        )
    }
}